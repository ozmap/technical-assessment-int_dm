version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3003:3003'
    environment:
      - MONGO_URI=mongodb://mongo:27017/oz-tech-test
    depends_on:
      - mongo

  mongo:
    image: mongo
    ports:
      - '27017:27017'
